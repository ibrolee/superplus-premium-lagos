import "@/components/reception/reception-responsive.css";
import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { Activity,ArrowRight,Cake,CalendarClock,CheckCircle2,ClipboardList,CreditCard,LayoutDashboard,RefreshCw,ScanLine,Search,ShieldCheck,UserPlus,Users,Wallet } from "lucide-react";
import { supabase } from "@/lib/supabase";

export const Route=createFileRoute('/reception-workspace')({component:ReceptionWorkspace});
type Member={id:string;full_name:string|null;phone:string|null;email:string|null;birth_day:number|null;birth_month:number|null};
type Membership={id:string;member_id:string;plan_name:string|null;start_date:string|null;end_date:string|null;status:string|null;payment_status:string|null};
const todayLagos=()=>{const p=new Intl.DateTimeFormat('en-GB',{timeZone:'Africa/Lagos',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date());const v=(name:string)=>p.find(item=>item.type===name)?.value||'';return `${v('year')}-${v('month')}-${v('day')}`;};
const plusDays=(date:string,days:number)=>{const value=new Date(`${date}T12:00:00Z`);value.setUTCDate(value.getUTCDate()+days);return value.toISOString().slice(0,10);};
async function allRows<T>(table:'members'|'memberships',columns:string):Promise<T[]>{const rows:T[]=[];for(let offset=0;;offset+=500){const {data,error}=await supabase.from(table).select(columns).order('id',{ascending:true}).range(offset,offset+499);if(error)throw error;const batch=(data||[]) as T[];rows.push(...batch);if(batch.length<500)return rows;}}
// Preserve original expiry-reminder eligibility: pending/unpaid/provisional access must not generate paid renewal reminders.
function eligibleReminder(plan:Membership,day:string){const start=plan.start_date?.slice(0,10)||'';const end=plan.end_date?.slice(0,10)||'';const status=(plan.status||'').toLowerCase();const payment=(plan.payment_status||'').toLowerCase();return Boolean(start&&end&&start<=day&& !['cancelled','canceled','paused','inactive','expired','void'].includes(status)&& !['failed','unpaid','pending','refunded','cancelled','canceled','void'].includes(payment));}
const quickActions=[
 {label:'New member',detail:'One-step registration and instant access (preview)',href:'/reception-register',icon:UserPlus,category:'REGISTER'},
 {label:'Renew membership',detail:'Find the existing member first',href:'/management-members',icon:RefreshCw,category:'MEMBERSHIP'},
 {label:'Payment request',detail:'Existing-member collections use the current protected workflow',href:'/management-payment-desk',icon:CreditCard,category:'COLLECTIONS'},
 {label:'Custom plan',detail:'Set custom days and price',href:'/management-custom-plan',icon:ClipboardList,category:'PLANS'},
 {label:'Standard plan',detail:'Existing standard-plan tool',href:'/management-standard-plan',icon:UserPlus,category:'PLANS'},
 {label:'Member directory',detail:'Find profiles, plans and expiry dates',href:'/management-members',icon:Users,category:'MEMBERS'},
];
function ReceptionWorkspace(){
 const [authorized,setAuthorized]=useState(false);const [role,setRole]=useState('');const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [refresh,setRefresh]=useState(0);const [members,setMembers]=useState<Member[]>([]);const [plans,setPlans]=useState<Membership[]>([]);const [search,setSearch]=useState('');
 const today=todayLagos();const cutoff=plusDays(today,7);
 useEffect(()=>{let cancelled=false;setAuthorized(false);setRole('');void(async()=>{setLoading(true);setError('');try{
  const {data:auth,error:authError}=await supabase.auth.getUser();if(authError||!auth.user)throw new Error('Sign in through reception or admin portal first.');
  const {data:staff,error:staffError}=await supabase.from('staff_users').select('role,active').eq('auth_user_id',auth.user.id).maybeSingle();if(staffError)throw staffError;
  const nextRole=String(staff?.role||'').toLowerCase();if(!staff?.active||!['reception','admin','owner','manager'].includes(nextRole))throw new Error('An active reception or management account is required.');
  if(cancelled)return;setRole(nextRole);setAuthorized(true);
  const [people,memberships]=await Promise.all([allRows<Member>('members','id,full_name,phone,email,birth_day,birth_month'),allRows<Membership>('memberships','id,member_id,plan_name,start_date,end_date,status,payment_status')]);
  if(!cancelled){setMembers(people);setPlans(memberships);}
 }catch(cause){if(!cancelled){setAuthorized(false);setRole('');setError(cause instanceof Error?cause.message:'Unable to load reception workspace.');}}finally{if(!cancelled)setLoading(false);}})();return()=>{cancelled=true;};},[refresh]);
 const counts=useMemo(()=>{const active=new Set<string>();const expired=new Set<string>();for(const plan of plans){const start=plan.start_date?.slice(0,10);const end=plan.end_date?.slice(0,10);const status=String(plan.status||'').toLowerCase();if(!start||!end||['paused','cancelled'].includes(status))continue;if(start<=today&&end>=today)active.add(plan.member_id);else if(end<today)expired.add(plan.member_id);}return{active:active.size,expired:[...expired].filter(id=>!active.has(id)).length};},[plans,today]);
 const birthdaysToday=useMemo(()=>members.filter(m=>m.birth_month===Number(today.slice(5,7))&&m.birth_day===Number(today.slice(8,10))).length,[members,today]);
 const expiringReminders=useMemo(()=>{const ids=new Set(members.map(m=>m.id));const grouped=new Map<string,Membership[]>();for(const p of plans){const prior=grouped.get(p.member_id)||[];prior.push(p);grouped.set(p.member_id,prior);}let count=0;for(const p of plans){const expiry=p.end_date?.slice(0,10)||'';if(!expiry||expiry<today||expiry>cutoff||!eligibleReminder(p,today)||!ids.has(p.member_id))continue;const nextDay=plusDays(expiry,1);const renewed=(grouped.get(p.member_id)||[]).some(other=>other.id!==p.id&&other.plan_name===p.plan_name&&eligibleReminder(other,nextDay)&&(other.start_date?.slice(0,10)||'')<=nextDay&&(other.end_date?.slice(0,10)||'')>expiry);if(!renewed)count++;}return count;},[members,plans,today,cutoff]);
 const results=useMemo(()=>{const needle=search.trim().toLowerCase();return needle.length<2?[]:members.filter(m=>[m.full_name,m.phone,m.email].some(value=>value?.toLowerCase().includes(needle))).slice(0,8);},[members,search]);
 const management=['admin','owner','manager'].includes(role);
 const dateLabel=new Intl.DateTimeFormat('en-NG',{timeZone:'Africa/Lagos',dateStyle:'full'}).format(new Date());
 const snapshot=[{label:'Registered members',value:members.length,icon:Users,href:null},{label:'Currently active',value:counts.active,icon:CheckCircle2,href:null},{label:'Plans expiring soon',value:expiringReminders,icon:CalendarClock,href:'/management-communications#renewal-messages'},{label:'Birthdays today',value:birthdaysToday,icon:Cake,href:'/management-communications#birthday-messages'},{label:'Expired members',value:counts.expired,icon:Activity,href:null}];
 return <main className="reception-responsive min-h-screen bg-[#f4f6f1] px-4 py-7 text-[#193327] sm:px-7 lg:px-10 lg:py-10"><div className="mx-auto max-w-[1400px] space-y-7">
  <header className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-[11px] font-black uppercase tracking-[.22em] text-[#679361]">Super Plus Fitness / Front desk</p><h1 className="mt-2 text-3xl font-black tracking-tight sm:text-4xl">Reception Dashboard</h1><p className="mt-2 text-sm text-[#647468]">Daily tasks, member information and reminders in one place.</p></div><button type="button" onClick={()=>setRefresh(n=>n+1)} disabled={loading} className="inline-flex items-center gap-2 rounded-xl border border-[#d8e2d5] bg-white px-4 py-3 text-sm font-bold disabled:opacity-50"><RefreshCw size={17} className={loading?'animate-spin':''}/> Refresh overview</button></header>
  {error&&<div role="alert" className="rounded-2xl border border-red-200 bg-red-50 p-5 text-sm text-red-800">{error} <a href="/portal" className="font-bold underline">Choose portal</a></div>}
  {loading&&<p role="status" className="rounded-2xl border border-[#dce8d9] bg-white p-6 text-sm">Checking access and loading member information…</p>}
  {!loading&&authorized&&!error&&<>
   <section className="relative overflow-hidden rounded-[28px] bg-[#193b2a] p-6 text-white sm:p-8 lg:p-9"><div className="pointer-events-none absolute -right-16 -top-24 size-72 rounded-full border-[48px] border-[#b8ee73]/10" aria-hidden="true"/><div className="relative flex flex-wrap items-center justify-between gap-7"><div className="max-w-2xl"><span className="inline-flex items-center gap-2 rounded-full border border-[#b8ee73]/40 bg-white/10 px-3 py-1.5 text-[11px] font-black uppercase tracking-wider text-[#b8ee73]"><span className="size-2 rounded-full bg-[#b8ee73]"/> Front desk workspace</span><h2 className="mt-5 text-3xl font-black leading-tight sm:text-4xl">Ready for today's members.</h2><p className="mt-3 text-sm text-[#c7ddca]">{dateLabel} · Lagos time</p><p className="mt-3 max-w-xl text-sm leading-6 text-[#d2e2d4]">Scan member QR codes, find existing members and register genuine first-time visitors. The new one-step registration is preview-only until the secure server release is approved.</p></div><div className="flex w-full flex-col gap-3 sm:w-auto"><a href="/reception-checkin" className="inline-flex items-center justify-center gap-2 rounded-xl bg-[#b8ee73] px-6 py-4 text-sm font-black text-[#173326] hover:bg-[#d0f59d]"><ScanLine size={19}/> Open QR scanner <ArrowRight size={16}/></a><a href="/reception-register" className="inline-flex items-center justify-center gap-2 rounded-xl border border-white/35 bg-white/10 px-6 py-3 text-sm font-bold text-white hover:bg-white/20"><UserPlus size={17}/> New registration preview</a></div></div></section>
   <section aria-label="Membership snapshot"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><p className="text-[11px] font-black uppercase tracking-widest text-[#679361]">At a glance</p><h2 className="mt-1 text-xl font-black">Member snapshot</h2></div><a href="/management-members" className="inline-flex items-center gap-2 text-xs font-bold text-[#356942]">View directory <ArrowRight size={15}/></a></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{snapshot.map(({label,value,icon:Icon,href})=>{const content=<><span className="flex items-start justify-between gap-2 text-xs font-semibold leading-5 text-[#607366]"><span>{label}</span><Icon size={18} className="shrink-0 text-[#54815c]"/></span><span className="mt-4 block text-3xl font-black tabular-nums">{value.toLocaleString('en-NG')}</span>{href&&<span className="mt-3 inline-flex items-center gap-1 text-[11px] font-bold text-[#356942]">Open reminders <ArrowRight size={12}/></span>}</>;const style='block min-w-0 rounded-[20px] border border-[#dce8d9] bg-white p-5 transition hover:border-[#a8c9a1]';return href?<a key={label} href={href} className={style}>{content}</a>:<div key={label} className={style}>{content}</div>;})}</div></section>
   <div className="grid gap-5 xl:grid-cols-[minmax(0,1.35fr)_minmax(320px,.65fr)]"><section className="min-w-0 rounded-[24px] border border-[#dce8d9] bg-white p-5 sm:p-7"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[11px] font-black uppercase tracking-widest text-[#679361]">Directory search</p><h2 className="mt-2 text-2xl font-black">Find a member</h2><p className="mt-2 text-sm text-[#607366]">Search by name, phone or email and open the member's profile.</p></div><a href="/management-members" className="inline-flex items-center gap-2 rounded-xl border border-[#d8e2d5] px-3 py-2.5 text-xs font-bold text-[#356942]">Full directory <ArrowRight size={15}/></a></div><label className="mt-5 flex items-center gap-3 rounded-xl border border-[#d8e2d5] bg-[#f7faf4] px-4"><Search size={19} className="shrink-0 text-[#54815c]"/><span className="sr-only">Search reception members</span><input type="search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Enter at least two characters…" className="w-full min-w-0 bg-transparent py-4 text-sm outline-none"/></label>{search.trim().length>=2?<div aria-live="polite" className="mt-3 space-y-2">{results.length?results.map(member=><a key={member.id} href={`/reception-member/${encodeURIComponent(member.id)}`} className="flex min-w-0 flex-wrap items-center justify-between gap-3 rounded-xl border border-[#e6ede2] bg-[#fbfcfa] px-4 py-3 hover:border-[#9fca91]"><span className="min-w-0 flex-1"><strong className="block truncate text-sm">{member.full_name||'Unnamed member'}</strong><span className="mt-1 block truncate text-xs text-[#637469]">{member.phone||member.email||'No contact recorded'}</span></span><span className="inline-flex items-center gap-1 text-xs font-bold text-[#356942]">View profile <ArrowRight size={14}/></span></a>):<p className="py-4 text-sm text-[#637469]">No matching members in the loaded directory.</p>}{results.length===8&&<p className="text-xs text-[#637469]">Showing up to eight matches. Use the full directory for more.</p>}</div>:<p className="mt-4 text-xs text-[#849387]">Tip: The top search also finds reception tools.</p>}</section><section className="rounded-[24px] border border-[#dce8d9] bg-[#eaf4e4] p-5 sm:p-7"><p className="text-[11px] font-black uppercase tracking-widest text-[#4f7a4e]">Member care</p><h2 className="mt-2 text-2xl font-black">Follow up today</h2><p className="mt-2 text-sm leading-6 text-[#586e5a]">Keep birthdays and approaching renewals visible.</p><div className="mt-5 space-y-3"><a href="/management-communications#renewal-messages" className="flex items-center justify-between gap-3 rounded-2xl bg-white p-4 hover:shadow-sm"><span className="flex items-center gap-3"><CalendarClock size={20} className="text-[#427a43]"/><span className="text-sm font-bold">Expiry reminders</span></span><span className="text-xl font-black tabular-nums">{expiringReminders}</span></a><a href="/management-communications#birthday-messages" className="flex items-center justify-between gap-3 rounded-2xl bg-white p-4 hover:shadow-sm"><span className="flex items-center gap-3"><Cake size={20} className="text-[#427a43]"/><span className="text-sm font-bold">Today's birthdays</span></span><span className="text-xl font-black tabular-nums">{birthdaysToday}</span></a></div><a href="/management-communications" className="mt-5 inline-flex items-center gap-2 text-sm font-black text-[#315b38]">Open reminders <ArrowRight size={16}/></a></section></div>
   <section aria-label="Reception quick actions"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><p className="text-[11px] font-black uppercase tracking-widest text-[#679361]">Everyday tools</p><h2 className="mt-1 text-2xl font-black">Quick actions</h2></div><a href="/management-operations" className="inline-flex items-center gap-2 text-xs font-bold text-[#356942]">All operations <ArrowRight size={15}/></a></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{quickActions.map(({label,detail,href,icon:Icon,category})=><a key={label} href={href} className="group flex min-h-36 min-w-0 flex-col justify-between rounded-[20px] border border-[#dce8d9] bg-white p-5 transition hover:-translate-y-0.5 hover:border-[#9fca91] hover:shadow-lg"><span className="flex items-center justify-between gap-2"><span className="rounded-xl bg-[#edf6e7] p-2.5 text-[#427a43]"><Icon size={20}/></span><ArrowRight size={17} className="text-[#7d9780] transition group-hover:translate-x-1"/></span><span><span className="text-[10px] font-black uppercase tracking-widest text-[#68905d]">{category}</span><strong className="mt-1 block text-lg">{label}</strong><span className="mt-1 block text-sm leading-5 text-[#647468]">{detail}</span></span></a>)}</div></section>
   <div className="flex flex-wrap items-start justify-between gap-4 rounded-2xl border border-[#dce8d9] bg-white p-5 text-xs leading-6 text-[#5b705d]"><p className="flex max-w-3xl items-start gap-2"><ShieldCheck size={18} className="mt-1 shrink-0 text-[#427a43]"/><span>Member counts reflect recorded dates and status, not verified revenue. The proposed instant-registration flow activates provisional access immediately but keeps the collection pending until a different manager reconciles it. Existing payment-request approvals remain unchanged until a separate migration is approved.</span></p>{management&&<div className="flex flex-wrap gap-3"><a href="/management-revenue" className="inline-flex items-center gap-1 font-bold text-[#356942]"><Wallet size={15}/> Revenue records</a><a href="/admin-workspace" className="inline-flex items-center gap-1 font-bold text-[#356942]"><LayoutDashboard size={15}/> Admin dashboard</a></div>}</div>
  </>}
 </div></main>;
}
